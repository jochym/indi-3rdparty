13. **GOTO via EQUATORIAL_EOD_COORD ignored even after Sync:** In simulation, after performing a 1-star `Sync`, subsequent GOTO commands to `EQUATORIAL_EOD_COORD` do not initiate motion (only sidereal tracking drift is observed). Motion is only achievable via direct `HORIZONTAL_COORD` commands.
14. **Driver Source Typo:** A typo in `celestronaux.cpp` (`proportionalTerm` instead of `propotionalTerm`) prevented the driver from building with recent INDI library versions. (Fixed by agent to enable testing).
